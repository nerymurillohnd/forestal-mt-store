{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read f; [ -n \"$f\" ] && echo \"$f\" | grep -qE '\\.(astro|ts|tsx|js|mjs|cjs|css|md|mdx|json)$' && pnpm --prefix \"$CLAUDE_PROJECT_DIR\" exec prettier --write \"$f\" 2>/dev/null || true; }"
          }
        ]
      }
    ]
  }
}
